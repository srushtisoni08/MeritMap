# ── Backend API URL ───────────────────────────────────────────────────────────
# Local dev — points to your Express server
NEXT_PUBLIC_API_URL=http://localhost:4000

# Production — replace with your deployed backend URL (Railway / Render)
# NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app